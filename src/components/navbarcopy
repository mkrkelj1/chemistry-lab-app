import React from 'react'
import { Link } from 'react-router-dom'

// Exports to Header.js

// wrap in container to center 

const NavigationBar = () => (
	<React.Fragment>
			    	
	    <nav>
	     <Link to='/'>Home</Link>
	     <br/>
	     <Link to='/courses'>Courses</Link>
	    </nav>
    </React.Fragment>
)

export default NavigationBar